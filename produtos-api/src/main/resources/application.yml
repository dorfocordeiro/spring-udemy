spring:
  application:
    admin: Produtos API

  datasource:
    url: jdbc:h2:mem:produtosdb
    username: sa
    password: admin

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true
      path: /h2-console